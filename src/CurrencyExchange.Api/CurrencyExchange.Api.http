@httpHost = http://localhost:5045
@httpsHost = https://localhost:7039

@walletId = 1
@amount = 50
@fromCurrency = USD
@toCurrency = EUR

### Get all currencies
GET {{httpHost}}/Currency
Accept: application/json

### Create a wallet
POST {{httpHost}}/Wallet
Content-Type: application/json
Accept: application/json

{
  "name": "Demo Wallet"
}

### List all wallets
GET {{httpHost}}/Wallet
Accept: application/json

### Get a wallet by id
GET {{httpHost}}/Wallet/{{walletId}}
Accept: application/json

### Deposit funds (currency must exist)
POST {{httpHost}}/Wallet/{{walletId}}/deposit
Content-Type: application/json
Accept: application/json

{
  "currencyCode": "{{fromCurrency}}",
  "amount": {{amount}}
}

### Withdraw funds
POST {{httpHost}}/Wallet/{{walletId}}/withdraw
Content-Type: application/json
Accept: application/json

{
  "currencyCode": "{{fromCurrency}}",
  "amount": 25
}

### Exchange funds (USD -> EUR)
POST {{httpHost}}/Wallet/{{walletId}}/exchange
Content-Type: application/json
Accept: application/json

{
  "fromCurrencyCode": "{{fromCurrency}}",
  "toCurrencyCode": "{{toCurrency}}",
  "amount": {{amount}}
}

### Get wallet transactions (optional paging with take/skip)
GET {{httpHost}}/Wallet/{{walletId}}/transactions?take=20&skip=0
Accept: application/json

### (Optional) Switch to HTTPS host
GET {{httpsHost}}/Currency
Accept: application/json